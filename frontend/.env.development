# Local development defaults — committed, no secrets.
# Override individual vars in .env.local (git-ignored) without editing this file.
VITE_APP_ENV=DEV
VITE_USE_MOCK_API=true
VITE_API_BASE_URL=http://localhost:4000
# Cognito intentionally left blank → demo auth bypass (useAuth mock)
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
