# Production build defaults — committed, placeholders only.
# Real values are injected by the CI/CD pipeline (CodeBuild env vars / SSM) and
# must NEVER be committed here.  See CLAUDE.md §5.4 (no secrets in source).
VITE_APP_ENV=PROD
VITE_USE_MOCK_API=false
VITE_API_BASE_URL=REPLACE_WITH_API_GATEWAY_URL
VITE_COGNITO_USER_POOL_ID=REPLACE_WITH_USER_POOL_ID
VITE_COGNITO_CLIENT_ID=REPLACE_WITH_CLIENT_ID
